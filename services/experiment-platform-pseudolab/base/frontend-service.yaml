apiVersion: v1
kind: Service
metadata:
  name: experiment-platform-frontend
spec:
  type: ClusterIP
  selector:
    app: experiment-platform
    component: frontend
  ports:
    - port: 80
      targetPort: 80
