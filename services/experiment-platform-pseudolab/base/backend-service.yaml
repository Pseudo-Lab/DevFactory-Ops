apiVersion: v1
kind: Service
metadata:
  name: experiment-platform-backend
spec:
  type: ClusterIP
  selector:
    app: experiment-platform
    component: backend
  ports:
    - port: 8000
      targetPort: 8000
