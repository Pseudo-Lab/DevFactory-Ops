apiVersion: v1
kind: Service
metadata:
  name: hackathon-platform-web
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  selector:
    app: hackathon-platform
    component: web
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
